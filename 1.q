function max(x:int,y:int):int
    if(x<y)
        return x;
        else
        return y;
        end
    end

        
var i:int;
i=0;    # iterator
while(i<10)
    puti(max(i,5));
    i=i+1;
    end

puts("PI=");
putr(3.14159);